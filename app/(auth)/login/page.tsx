import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login",
  description: "Login to Feliz Feet Client",
};
const Login = () => {
	return (
		<div>Login</div>
	)
}

export default Login